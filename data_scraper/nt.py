#!/usr/bin/env python
# -*- coding: utf-8 -*-

from .base import BaseData


class NtData(BaseData):
    """Website is so back that we're going to have to come back to this one and
    copy and paste data from a PDF into a csv file. No point doing it until the
    very last minute.
    """
    def ntData(self):
        pass
